clc;clear all;close all;

syms y(t)
eqn = diff(y, t) == 4*y;
cond = y(0) == 5;
ySol(t) = dsolve(eqn)

ySol(t) = dsolve(eqn, cond)

ySol(t) = dsolve('D2y + 2*Dy + y = 0')

clc;clear all;close all;

syms y(t) a b
eqn 